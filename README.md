# todo-vanilla

A small, dependency-free Todo application using vanilla JavaScript and localStorage.

Why include on resume
- Demonstrates DOM manipulation, state persistence, clean UI, accessibility and keyboard support.

How to run
- Open `index.html` in a browser.